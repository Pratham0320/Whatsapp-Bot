import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { Navbar } from "@/components/navbar"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "WhatsApp AI Bot - Automate Your Business Communication",
  description:
    "Powerful WhatsApp automation bots with AI integration. Auto-replies, lead generation, and ChatGPT integration for your business.",
  keywords: "WhatsApp automation, AI chatbot, business automation, lead generation",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        <main>{children}</main>
      </body>
    </html>
  )
}
